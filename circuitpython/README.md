# CircuitPython Programs

Each subfolder contains a standalone `code.py` for the Pico RGB Keypad.

To run one, copy its `code.py` to the root of the `CIRCUITPY` drive.

The Windows launcher is the only project that also needs `boot.py` and `launcher_config.json` on the Pico.
